service:
  replicaCount: 2
  global:
    # Production consumes an explicitly published stable tag such as `release`.
    tag: release
    env:
      APP_ENV: production
      DB_SCHEMA: core_prod
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: 1
      memory: 768Mi
  pdb:
    minAvailable: 1
